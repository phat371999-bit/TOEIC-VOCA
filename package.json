{
  "name": "gemini-proxy-app",
  "version": "1.0.0",
  "description": "React + Vite + Express proxy for Google Generative AI",
  "main": "server/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  }
}
